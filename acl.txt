# ─────────────────────────────────────────
# 端口控制
# ─────────────────────────────────────────
#block port 135                          # 阻止危险端口
#block port 445
#allow port 22                           # SSH 端口放行（跳过后续 block/proxy）

# ─────────────────────────────────────────
# IP / CIDR 控制
# ─────────────────────────────────────────
#allow cidr 10.0.0.0/8                  # 内网网段直连
#allow cidr 192.168.0.0/16
#block ip 1.2.3.4                       # 阻止特定 IP
#proxy ip 8.8.8.8 direct                # 指定 IP 强制直连（不走代理）
#proxy cidr 172.16.0.0/12 internal      # 特定网段走 internal 代理

# ─────────────────────────────────────────
# 域名控制（广告拦截 / 强制代理）
# ─────────────────────────────────────────
#block domain *.adnetwork.com           # 拦截广告域名（含所有子域名）
#block domain tracker.example.com       # 拦截精确域名

# ── vivo 浏览器隐私上报/云查拦截(逆向 v30.7.0.0 实测) ──
# 注意: 以下两个是 .com 不是 .com.cn, *.vivo.com.cn 规则盖不住!
block domain browsercloud.vivo.com        # Speedy 云加速网关,全流量经此转发(proxy.do/check.do),日志实证
block domain kernelapi.vivo.com           # 配置/名单下发容灾域名(.com 版,别只拦 .com.cn)
# 云查: 每打开一个页面 POST 完整 URL+搜索词(isLegitimate.do)
block domain browsercrp.vivo.com.cn
# 行为埋点 / 广告 / 风控上报
block domain browserreport.vivo.com.cn
block domain stvideoreport.vivo.com.cn
block domain videobacklog.vivo.com.cn
block domain adreq.vivo.com.cn
block domain adlog.vivo.com.cn
block domain sentinelapi.vivo.com.cn
block domain st-browser.vivo.com.cn
block domain usrsys.vivo.com.cn
block domain usrsysjoint.vivo.com.cn
block domain gaia-mix-prd.vivo.com.cn
block domain browsersubscribe.vivo.com.cn
block domain browserconf.vivo.com.cn
block domain kernelapi.vivo.com.cn        # 名单下发 multiget.do(拦后名单停更)
block domain kernelstatic.vivo.com.cn     # 名单 zip 实际下载源(multiget 返回的 URL 主机)
block domain fuli.vivo.com.cn
# AI 搜索/助手(上传搜索词与 URL,拦后 AI 功能失效)
block domain gptkit-proxy.vivo.com.cn
block domain ai-search-partner-prd.vivo.com.cn
block domain copilot-api-auth-prd.vivo.com.cn
# 第三方(字节)URL 安全扫描
block domain scc.bytedance.com
# 错误页/新标签页「精彩推荐」信息流:h5 是 feeds 模块页面,数据走 browsercrp/loadAll.do(已堵),
# 其余为内容分发/热榜/素材 CDN,堵死后推荐栏整栏空白
block domain h5.vivo.com.cn
block domain browserfeeds.vivo.com.cn
block domain fdcontentdispatch.vivo.com.cn
block domain feedscontent-hot-dst.vivo.com.cn
block domain fc-news-detail.vivo.com.cn
block domain contentstatic.vivo.com.cn

#allow domain internal.corp             # 公司内部域名放行（不拦截、不代理）

# ─────────────────────────────────────────
# 代理路由（按域名选择上游）
# ─────────────────────────────────────────
#proxy domain *.google.com primary      # Google 服务走 primary 代理
#proxy domain twitter.com jp            # Twitter 走 jp 代理
#proxy domain *.netflix.com us          # Netflix 走 us 代理

# 特殊值 "direct" 表示强制直连（跳过代理链）
#proxy port 22 direct                   # SSH 端口强制直连


proxy domain api.tripdata.app ss-local

block domain cdn.wwads.cn

block domain ads.mozilla.org
