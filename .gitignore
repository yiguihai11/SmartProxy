# build artifacts
/build/
/dist/

# stale binaries at repo root
/socks5-router

# logs & runtime sockets
*.log
*.sock

# runtime-generated admin TLS cert pair (auto-created next to the config file)
admin.crt
admin.key

# editor / OS cruft
.DS_Store
*.swp

# local tooling config
.claude/settings.local.json

# Claude Code local tooling config
.claude/
